# Copy to .env and fill in as future providers are added.
# TUSHARE_TOKEN=

# OpenAI-compatible endpoint used by the AI Analysis step (research report).
LLM_API_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Existing OneProCloud-compatible names are also accepted by the research CLI.
# LLM_ONEPROCLOUD_BASE_URL=
# LLM_ONEPROCLOUD_API_KEY=
# LITELLM_MODEL=
# JoinQuant is used only for historical tradability fields (paused/limits).
# Never commit real credentials.
JQ_USERNAME=
JQ_PASSWORD=
